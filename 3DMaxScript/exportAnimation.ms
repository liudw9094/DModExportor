fn GetAnimationTransform obj time =
(

)
fn WriteAnimationTransform obj hFile=
(
	local transform
	controller = obj.transform.controller
	numkey = numSelKeys controller
	for i = 1 to numkey do
	(
		
	)
)